import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  symbol: {
    marginRight: 10,
    fontWeight: 600,
    fontSize: 22,
  },
  name: {
    fontSize: 18,
  },
});
